name: Build native ZPEX plugin

on:
  push:
  workflow_dispatch:

jobs:
  jar:
    name: JVM plugin JAR
    runs-on: ubuntu-24.04

    steps:
      - name: Check out zpe.lib.docx
        uses: actions/checkout@v5

      - name: Verify JVM plugin JAR
        run: |
          test -f build/zpe.lib.docx.jar
          unzip -tq build/zpe.lib.docx.jar

      - name: Upload JVM plugin
        uses: actions/upload-artifact@v4
        with:
          name: zpe-lib-docx-jvm
          path: build/zpe.lib.docx.jar
          if-no-files-found: error
          retention-days: 30

  build:
    name: ${{ matrix.name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS ARM64 dylib
            os: macos-14
            artifact: zpe-lib-docx-macos-arm64
            output: build/native/zpe.lib.docx.dylib
          - name: Windows x64 DLL
            os: windows-2022
            artifact: zpe-lib-docx-windows-x64
            output: build/native/zpe.lib.docx.dll
          - name: Linux x64 shared library
            os: ubuntu-24.04
            artifact: zpe-lib-docx-linux-x64
            output: build/native/zpe.lib.docx.so

    runs-on: ${{ matrix.os }}

    steps:
      - name: Check out zpe.lib.docx
        uses: actions/checkout@v5

      - name: Set up GraalVM Native Image
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: "25"
          distribution: graalvm-community
          github-token: ${{ secrets.GITHUB_TOKEN }}
          native-image-job-reports: "true"

      - name: Enable the MSVC x64 toolchain
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          & "${env:ProgramFiles}\Microsoft Visual Studio\2022\Enterprise\Common7\Tools\Launch-VsDevShell.ps1" -Arch amd64 -HostArch amd64
          "PATH=$env:PATH" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "INCLUDE=$env:INCLUDE" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "LIB=$env:LIB" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "LIBPATH=$env:LIBPATH" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Download Apache POI dependencies
        shell: bash
        run: ./download-native-dependencies.sh build/native-dependencies

      - name: Build native DOCX plugin
        shell: bash
        env:
          ZPE_DEPENDENCY_DIR: build/native-dependencies
          BUILD_STATIC_PLUGIN: "false"
        run: ./build-native.sh

      - name: Verify native plugin
        shell: bash
        run: test -f "${{ matrix.output }}"

      - name: Upload native plugin
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.output }}
          if-no-files-found: error
          retention-days: 30

  package:
    name: Package and deploy all platforms
    needs:
      - jar
      - build
    runs-on: ubuntu-24.04

    steps:
      - name: Check out zpe.lib.docx
        uses: actions/checkout@v5

      - name: Download JVM plugin
        uses: actions/download-artifact@v5
        with:
          name: zpe-lib-docx-jvm
          path: release/zpe.lib.docx

      - name: Download macOS plugin
        uses: actions/download-artifact@v5
        with:
          name: zpe-lib-docx-macos-arm64
          path: release/zpe.lib.docx/native/macos-arm64

      - name: Download Windows plugin
        uses: actions/download-artifact@v5
        with:
          name: zpe-lib-docx-windows-x64
          path: release/zpe.lib.docx/native/windows-x64

      - name: Download Linux plugin
        uses: actions/download-artifact@v5
        with:
          name: zpe-lib-docx-linux-x64
          path: release/zpe.lib.docx/native/linux-x64

      - name: Assemble all-platform ZIP
        run: |
          cp README.md LICENSE release/zpe.lib.docx/
          cd release
          zip -9 -r zpe.lib.docx.zip zpe.lib.docx
          unzip -l zpe.lib.docx.zip

      - name: Upload all-platform ZIP
        uses: actions/upload-artifact@v4
        with:
          name: zpe-lib-docx-all-platforms
          path: release/zpe.lib.docx.zip
          if-no-files-found: error
          retention-days: 30

      - name: Deploy all-platform ZIP with rsync
        env:
          SFTP_HOST: ${{ secrets.SFTP_HOST }}
          SFTP_PORT: ${{ secrets.SFTP_PORT }}
          SFTP_USERNAME: ${{ secrets.SFTP_USERNAME }}
          SFTP_PRIVATE_KEY: ${{ secrets.SFTP_PRIVATE_KEY }}
          SFTP_HOST_KEY: ${{ secrets.SFTP_HOST_KEY }}
          SFTP_DESTINATION: ${{ secrets.SFTP_DESTINATION_DOCX }}
        run: |
          test -n "$SFTP_HOST"
          test -n "$SFTP_USERNAME"
          test -n "$SFTP_PRIVATE_KEY"
          test -n "$SFTP_HOST_KEY"
          test -n "$SFTP_DESTINATION"
          port="${SFTP_PORT:-22}"
          install -d -m 700 "$HOME/.ssh"
          printf '%b\n' "$SFTP_PRIVATE_KEY" | tr -d '\r' > "$HOME/.ssh/docx_deploy"
          printf '%b\n' "$SFTP_HOST_KEY" | tr -d '\r' > "$HOME/.ssh/known_hosts"
          chmod 600 "$HOME/.ssh/docx_deploy" "$HOME/.ssh/known_hosts"
          if ! ssh-keygen -y -P "" -f "$HOME/.ssh/docx_deploy" > /dev/null; then
            echo "::error::SFTP_PRIVATE_KEY is invalid or passphrase-protected."
            exit 1
          fi
          rsync -az --progress \
            -e "ssh -p $port -i $HOME/.ssh/docx_deploy -o IdentitiesOnly=yes" \
            release/zpe.lib.docx.zip \
            "$SFTP_USERNAME@$SFTP_HOST:$SFTP_DESTINATION"
